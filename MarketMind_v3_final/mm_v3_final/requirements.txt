# MarketMind v3 — dependencies (pinned for reproducibility)
# pip install -r requirements.txt

# ── Price data ─────────────────────────────────────────────────────────────
yfinance>=0.2.40,<0.3.0           # primary price source for all asset classes
pytz>=2024.1                       # timezone handling

# ── Alternate data sources ─────────────────────────────────────────────────
# All sourced via built-in urllib — no extra SDK needed for:
#   FRED, Finnhub, Alpha Vantage, EIA, CoinGecko, NSE, RBI DBIE, NewsAPI

# ── LLM clients (pinned to minor versions) ────────────────────────────────
openai>=1.50,<2.0                  # gpt-4o-2024-11-20 + gpt-4o-mini-2024-07-18
anthropic>=0.36,<0.40             # claude-opus-4-6 (coordinator)

# ── Schema validation ─────────────────────────────────────────────────────
pydantic>=2.7,<3.0

# ── Async support ─────────────────────────────────────────────────────────
nest_asyncio>=1.6,<2.0            # safe asyncio.run() in Jupyter / FastAPI
aiohttp>=3.9,<4.0

# ── Numerical ─────────────────────────────────────────────────────────────
numpy>=1.26,<2.0
pandas>=2.2,<3.0

# ── Environment ───────────────────────────────────────────────────────────
python-dotenv>=1.0,<2.0

# ── Terminal output ────────────────────────────────────────────────────────
rich>=13.0,<14.0

# ── Testing ───────────────────────────────────────────────────────────────
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<0.24
